{
  "name": "bucket",
  "description": "Handy CRUD methods for working with Mongoose",
  "scripts": {
    "watch": "tsc --watch -p ./tsconfig.json",
    "build": "tsc -p ./tsconfig.json",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "mongoose": "^5.6.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/mongoose": "^5.5.6",
    "jest": "^24.0.0",
    "ts-jest": "^24.0.2"
  }
}
